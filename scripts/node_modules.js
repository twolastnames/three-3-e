var fs = require('fs');

var package = fs.readFileSync(__dirname + '/../package.json').toString()
var dependencies = JSON.parse(package).dependencies;
var names = Object.keys(dependencies);

fs.readdir(__dirname + '/../lib', function(err, items) {
  var name;
  while(name = names.shift()) {
    console.log(name);
  }
})
